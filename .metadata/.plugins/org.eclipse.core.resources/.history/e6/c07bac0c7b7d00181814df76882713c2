package entities;

public class Triangle {
	
	public double a;
	public double b;
	public double c;
	
	public double area() {
		p = (x.a + x.b + x.c) / 2.0;
		return Math.sqrt(p*(p - x.a)*(p - x.b)*(p - x.c));
	}
}
